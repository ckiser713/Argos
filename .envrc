# direnv configuration for Cortex project
# Auto-activates the Nix development shell when entering the directory
# 
# To use:
#   1. Install direnv: nix-env -iA nixpkgs.direnv
#   2. Add to your shell config: eval "$(direnv hook bash)" (or zsh/fish equivalent)
#   3. Run: direnv allow

use flake

# Optional: Set additional environment variables
export CORTEX_ENV=dev
export CORTEX_QDRANT_URL="http://localhost:6333"
export CORTEX_DB_URL="postgresql+psycopg://cortex:cortex@localhost:5432/cortex"

